apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-jupyter-workspace
  namespace: dis
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-nfs-jupyter
