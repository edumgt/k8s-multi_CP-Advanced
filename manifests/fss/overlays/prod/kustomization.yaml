apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patchesStrategicMerge:
  - infra-scale-patch.yaml

labels:
  - pairs:
      app.kubernetes.io/environment: prod
