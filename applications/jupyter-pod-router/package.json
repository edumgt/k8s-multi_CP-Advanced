{
  "name": "jupyter-pod-router",
  "version": "0.1.0",
  "private": true,
  "description": "Wildcard-host router for named Jupyter pods behind a headless service.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0",
    "http-proxy": "^1.18.1"
  }
}

